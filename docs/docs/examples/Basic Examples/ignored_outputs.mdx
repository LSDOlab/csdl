# Failing to Register Outputs Results in No Components

Here, a subsystem is added as in the previous example, but no outputs
are registered in the parent ``Model``.

This "dead code" is ignored when the CSDL compiler back end constructs a
computational model.

import WorkedExample from './../../worked_examples/ex_explicit_no_registered_output.mdx';

<WorkedExample />
